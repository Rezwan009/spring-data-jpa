spring:
  application:
    name: Spring Boot JPA
  datasource:
    url: jdbc:postgresql://localhost:5434/data_jpa
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update