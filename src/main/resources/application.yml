spring:
  application:
    name: Spring Boot JPA
  datasource:
    jdbcUrl: jdbc:postgresql://localhost:5434/data_jpa
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
app:
  db2:
    jdbcUrl: jdbc:postgresql://localhost:5434/data_jpa_2
    username: postgres
    password: postgres